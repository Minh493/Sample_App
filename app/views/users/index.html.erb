<% provide :title, "All users" %>
<h1><%= t ".alluser"%></h1>
<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 50 %>
      <%= link_to user.name, user %>
    </li>
  <%= render @users %>
  <% end %>
</ul>
<%= will_paginate %>
